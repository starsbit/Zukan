# Generate with: openssl rand -hex 32
SECRET_KEY=CHANGE_ME

# Used to construct the DATABASE_URL inside docker-compose.prod.yml
POSTGRES_PASSWORD=CHANGE_ME

# All other settings (AniList integration, tagger thresholds, token expiry,
# OCR, rate limits, etc.) are configurable via the admin panel after first login.
