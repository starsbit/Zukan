FROM node:22-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npx ng build --configuration e2e

FROM nginx:alpine
COPY --from=build /app/dist/zukan/browser /usr/share/nginx/html
COPY nginx.e2e.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
